import React from "react";
import PropTypes from 'prop-types';


 Text = (text) => { 
export class Text extends React.Component {

    render() {
        return (
            <p>{this.props.text}</p>
        )
    }
}